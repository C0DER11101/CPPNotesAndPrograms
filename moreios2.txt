Explanation of the .cpp file with the same name.

ios::showpos basically enables the showing the + sign before a number.

Example:

cout.setf(ios::showpos);
cout<<23.4<<endl;

output of these lines will be:

+23.4
